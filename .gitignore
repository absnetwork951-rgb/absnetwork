node_modules/
.next/
# Test-coverage output lives at the repo root only; keeping this anchored so
# it never ignores a source directory named "coverage" (e.g. app/admin/coverage).
/coverage/
.DS_Store
*.log
.env*
!.env.example
